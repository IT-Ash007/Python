def pattern(n): #user defined function
    for i in range(1,n+1): #control outer loop and number of rows
        for j in range(1,i+1): #control inner loop and number of columns
            print("*",end="")
        print("\n")
        
print("Please enter number of rows")
n = int(input())
print(pattern(n))
